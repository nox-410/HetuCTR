#include "hetu_gpu_table.h"

using namespace hetu;

void HetuGPUTable::preprocessIndex(index_t *data, size_t len) {
  return;
}

void HetuGPUTable::pushPull(embed_t *grad, embed_t *dst) {
  return;
}
